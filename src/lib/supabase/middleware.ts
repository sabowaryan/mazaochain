// Supabase middleware client removed — project now uses Clerk middleware
// This stub is kept to avoid breaking unused imports
export const createMiddlewareClient = () => {
  throw new Error('Supabase middleware replaced by Clerk. See src/middleware.ts');
};
